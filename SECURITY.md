# Security Policy

## 隐私与权限

FileAtlas 首版只读取用户主动选择的目录，不申请写入权限，不包含删除、移动或重命名执行功能。

项目不会主动上传：

- 文件内容
- 文件名
- 目录结构
- 文件哈希
- 扫描统计
- 本地快照

部署平台仍会像普通静态网站一样提供 HTML、CSS 和 JavaScript 资源。自建部署或离线构建可以进一步减少对第三方托管的依赖。

## 报告安全问题

请不要在公开 Issue 中附带真实目录结构、文件名、哈希或可能包含敏感信息的截图。可以通过 GitHub Security Advisory 私下报告安全问题。

报告中请包括：

- 受影响版本
- 浏览器与操作系统
- 复现步骤
- 预期行为与实际行为
- 是否涉及未授权读取、写入或网络发送

## 当前限制

- 浏览器扩展、被注入的脚本或被篡改的部署站点可能破坏本地处理的安全假设。
- 大于 128 MB 的重复候选只做抽样哈希，不能视为密码学意义上的完整相同确认。
- IndexedDB 数据受浏览器站点数据策略控制，可能被用户或浏览器清理。
