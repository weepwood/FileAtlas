# Contributing

## 本地验证

提交前请运行：

```bash
npm run lint
npm test
npm run build
```

## 设计约束

- 默认只读，写入能力必须独立授权。
- 不添加会收集文件或目录信息的分析 SDK。
- 不能把“相似名称”表述为“重复内容”。
- 抽样哈希结果必须与完整哈希结果区分。
- 大规模列表不得直接完整渲染到 DOM。
- 新增长耗时计算应优先放入 Web Worker。
- 破坏性功能必须先生成预览、检查冲突和保留操作日志。
